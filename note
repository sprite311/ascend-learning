1 为什么Vector（向量计算单元）	适合做激活函数、归一化等(Softmax LayerNorm )向量运算

3 推理引擎	TensorRT-LLM / vLLM	MindIE / vLLM-Ascend

4 时钟周期

5 人类常用的矩阵格式（如 NCHW），而要求数据必须按照特定的分形格式（如 NC1HWC0）排列

6 Softmax：计算注意力权重时要做指数运算和归一化
LayerNorm：每一层之后要做均值和方差的计算
GELU/SiLU：激活函数，是逐元素的非线性运算
Add：残差连接中的逐元素加法
这些操作有一个共同特点：不是矩阵乘法，而是逐元素或逐向量的操作。让 Cube 来干这活，就像用吊车搬鸡蛋——太浪费。

Vector 单元就是专门做这些精细活的。它一次可以对一整排数字（一个向量）做运算，效率远高于逐个处理。

7 HBM

8 芯片到底是如何实现计算和逻辑控制的

9量化